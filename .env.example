# Copy this file to `.env` and fill in your PostgreSQL connection string.
# For local dev on Render, use the EXTERNAL Database URL (host ends in .render.com).
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Optional: "true" forces SSL, "false" disables it. Auto-enabled for *.render.com.
# PGSSL=true
